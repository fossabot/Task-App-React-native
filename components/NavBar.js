import React, { Component } from 'react'
import {Container,Header, Title, Icon, Button,Text} from 'native-base'
import {View} from 'react-native'
export default class NavBar extends Component {

   render() {
        return (
            <Header>
               <Title>Dashboard</Title>
            </Header>
        )
   }
}